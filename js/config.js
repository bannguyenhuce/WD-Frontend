// config.js
// Thay đường dẫn bên dưới bằng Link Render của bạn (đã làm ở bài trước)
const API_URL = "https://wd-backend-api.onrender.com";

// Xuất biến này ra (nếu dùng module) hoặc để global
// Với HTML thuần, chỉ cần nhúng file này lên trước các file JS khác là dùng được biến API_URL